name: first workflow
on:
  push:
    branches:
      - master
jobs:
  test-job-1:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          echo "Sleeping for 30 seconds"
          sleep 30
          echo "done"
  test-job-2:
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        run: echo "executing step 1 from job2"
      - name: step-2
        run: echo "executing step2 from job2"
